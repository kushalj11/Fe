// Fibonacci sequence calculator
// Demonstrates: functions, recursion, if/else, arithmetic

fun fibonacci(n: int): int {
  if (n <= 1) {
    return n;
  } else {
    return fibonacci(n - 1) + fibonacci(n - 2);
  }
}

let i: int = 0;
while (i < 10) {
  print(fibonacci(i));
  i = i + 1;
}
